{
  "name": "trpr-react",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build:win": "SET GENERATE_SOURCEMAP=false && react-scripts build",
    "build:dev:win": "SET GENERATE_SOURCEMAP=true && react-scripts start"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
